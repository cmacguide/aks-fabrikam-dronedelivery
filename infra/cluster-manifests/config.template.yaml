# Generated by Copilot
# ConfigMap para armazenar valores de configuração do ambiente

apiVersion: v1
kind: ConfigMap
metadata:
  name: fabrikam-config
  namespace: default
data:
  app.name: "${APP_NAME}"
  environment.name: "${ENVIRONMENT_NAME}"
  azure.tenant.id: "${AZURE_TENANT_ID}"
  azure.subscription.id: "${AZURE_SUBSCRIPTION_ID}"

---
# Secret para valores sensíveis
apiVersion: v1
kind: Secret
metadata:
  name: fabrikam-secrets
  namespace: default
type: Opaque
stringData:
  k8s.rbac.entra.admin.group.object.id: "${K8S_RBAC_ENTRA_ADMIN_GROUP_OBJECT_ID}"
